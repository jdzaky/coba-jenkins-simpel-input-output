pipeline {
    agent any

    environment {
        GIT_CREDENTIALS = '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' 
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/jdzaky/coba-jenkins-simpel-input-output.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'pip install -r requirements.txt'
            }
        }
        stage('Run Application') {
            steps {
                sh 'python main.py &'
            }
        }
        stage('Expose with ngrok') {
            steps {
                sh 'D:\DOWNLOAD\Aplikasi exe\ngrok.exe http 6969'
            }
        }
    }
}
